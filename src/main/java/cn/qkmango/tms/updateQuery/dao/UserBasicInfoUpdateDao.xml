<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.qkmango.tms.updateQuery.dao.UserBasicInfoUpdateDao">


    <update id="updateStudentPassword">
        UPDATE t_student
        SET password = #{newPassword}
        WHERE id = #{id}
          and password = #{oldPassword}
    </update>

    <update id="updateTeacherPassword" parameterType="hashmap">

    </update>

    <update id="updateAdminPassword" parameterType="hashmap">

    </update>

</mapper>